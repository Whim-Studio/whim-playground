package com.whim.colony.domain;

/** The kinds of storyteller events that can befall the colony. */
public enum IncidentType {
    COLD_SNAP,
    HEAT_WAVE,
    RAID,
    RESOURCE_DROP,
    DISEASE,
    WANDERER_JOINS
}
