package com.tycoon.core;
// LOCAL-ONLY VERIFICATION STUB (Task 1 owns this) — never committed by Task 2.
public enum InterruptType {
    DEVELOPMENT_MILESTONE, EMPLOYEE_CRISIS, MARKET_SHIFT, GAME_RELEASED, MANUAL_PAUSE;
}
